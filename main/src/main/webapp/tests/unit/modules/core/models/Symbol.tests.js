describe('Symbol', function () {

    var Symbol;

    beforeEach(angular.mock.module('ALEX'));
    beforeEach(angular.mock.module('ALEX.core'));
    beforeEach(angular.mock.inject(function (_Symbol_) {
        Symbol = _Symbol_;
    }));

    it('should create a Symbol', function () {
    });

    it('should create a Symbol from an object representation', function () {
    });

    it('should create Symbol instance[s] from an http response', function () {
    });
});