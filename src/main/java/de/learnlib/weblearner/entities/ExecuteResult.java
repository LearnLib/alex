package de.learnlib.weblearner.entities;

/**
 * An Enumeration to determine if the AbstractSymbol was executed correctly or not.
 */
public enum ExecuteResult {

    /** The AbstractSymbol was executed correctly. */
    OK,

    /** While executing the AbstractSymbol something went wrong. */
    FAILED
}
